@model List<MoneyFlow.Entities.Services>


<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Type</th>
        </tr>
    </thead>
    <tbody>
            @foreach(var item in Model)
            {
             <tr>
                 <td>@item.Name</td>
                   <td>@item.Type</td>
                   <td>
                   <!--
              PROXIMAMENTE LOS BOTONES!
                 -->
                 </td>
             </tr>

            }
    </tbody>
</table>